# All possible options and their defaults: https://github.com/zitadel/zitadel/blob/main/cmd/setup/steps.yaml
FirstInstance:
  Org:
    Name: 'Percona'
    Human:
      # use the login admin@percona.<my.domain>
      Username: 'admin'
      Password: 'Password1!'
      DisplayName: 'Admin'